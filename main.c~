/*
  This is the wrapper file that
  will call the DeltaSigma code.
 */
#include <stdio.h>

int main(int argc, char **argv){
  FILE *fp;
  char buff[255];

  fp = fopen("test_data/matter_power_lin/k_h.txt","r");
  fscanf(fp,"%s\n",buff);
  printf("%s\n",buff);
}
